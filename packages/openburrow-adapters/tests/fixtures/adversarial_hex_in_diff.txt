--- a/src/hash.py
+++ b/src/hash.py
@@ -3,6 +3,13 @@
 HEX_DIGEST = "a1b2c3d4e5f6"
+SEED = "deadbeef0"
+LEGACY_ID = "0123456789abcdef"
+
+# Test data that happens to look exactly like a commit notice. In a unified
+# diff every line carries a prefix, so this can never match a line-leading
+# anchor — which is the property the parser relies on.
+Commit 1234567 this line is fixture data, not a git revision
 def digest(value):
     return blake2b(value).hexdigest()
